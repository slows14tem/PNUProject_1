version: '3'

services:
  react:
    image: 'upcoder/react-image'
    build:
      context: front2
      dockerfile: Dockerfile
    ports:
      - '3000:3000'

  spring:
    image: 'upcoder/myapp'
    build:
      context: project_1
      dockerfile: Dockerfile
    ports:
      - '8080:8080'
