version: '3'

services:
  react:
    image: react-image
    volumes:
      - ./front2:/app
    build:
      context: ./front2
      dockerfile: ./front2/Dockerfile
    ports:
      - 3000:3000

  spring:
    image: myorg/myapp
    build:
      context: ./project_1
      dockerfile: ./project_1/Dockerfile
    ports:
      - 8080:8080
